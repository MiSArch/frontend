/**
 * Interface representing a name. A name has a first name and a last name.
 * @interface
 * @property firstName - The first name.
 * @property lastName - The last name.
 */
export interface Name {
    firstName: string
    lastName: string
}
