overwrite: true
schema: "./temp-schema.graphql"
documents: "src/**/*.graphql"
generates:
    src/graphql/generated.ts:
        plugins:
            - "typescript"
            - "typescript-operations"
            - "typescript-graphql-request"
        config:
            onlyOperationTypes: true
